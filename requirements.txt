# Convenience pointer used by local dev tools and CI.
# For Docker builds the Dockerfile references requirements/base.txt directly.
# For local dev: pip install -r requirements.txt
-r requirements/dev.txt
